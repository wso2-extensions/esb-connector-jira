{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Jira API Get Issue Types Operation Response Schema",
  "description": "Schema representing the get issue types operation in Jira API",
  "properties": {
    "payload": {
      "description": "Response for the `getIssueTypes` method.",
      "properties": {}
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}
