{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Jira API Performs an Issue Transition Response Schema",
  "description": "Schema representing the output of the performs an issue transition operation in Jira API",
  "properties": {
    "payload": {
      "description": "Response for the `doTransition` method.",
      "properties": {}
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}