{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Jira API Get Issue Link by Id Operation Response Schema",
  "description": "Schema representing the output of the get issue link by id operation in Jira API",
  "properties": {
    "payload": {
      "description": "Response for the `getIssueLinkById` method.",
      "properties": {}
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}
