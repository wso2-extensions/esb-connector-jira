{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Jira API List Group User Picker Operation Response Schema",
  "description": "Schema representing the output of the list group user picker operation in Jira API",
  "properties": {
    "payload": {
      "description": "Response for the `listGroupUserPicker` method.",
      "type": "object",
      "properties": {}
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}
