{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Jira API Get Component Issues Count Operation Response Schema",
  "description": "Schema representing the output of the get component issues count operation in Jira API",
  "properties": {
    "payload": {
      "description": "Response for the `countComponentRelatedIssues` method.",
      "type": "object",
      "properties": {
        "self": {
          "description": "The URL for this count of issues for a component.",
          "type": "string"
        },
        "issueCount": {
          "description": "The count of issues assigned to a component.",
          "type": "number"
        }
      }
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}