{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Jira API Delete Comment Operation Response Schema",
  "description": "Schema representing the output of the delete comment operation in Jira API",
  "properties": {
    "payload": {
      "description": "Response for the `deleteComment` method.",
      "properties": {}
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}
