{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Jira API Update Issue Assignee Operation Response Schema",
  "description": "Schema representing the output of the update issue assignee operation in Jira API",
  "properties": {
    "payload": {
      "description": "Response for the `updateIssueAssignee` method.",
      "type": "object",
      "properties": {}
    },
    "attributes": {
      "description": "Metadata about the operation.",
      "properties": {
        "statusCode": {
          "description": "HTTP status code of the API response."
        }
      }
    },
    "headers": {
      "description": "HTTP headers of the API response.",
      "properties": {}
    }
  }
}